[workspace]
resolver = "3"
members = ["crates/ds-core", "crates/ds-cli"]

[workspace.package]
version = "0.1.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/jbenjoseph/ds"

[workspace.dependencies]
ds-core = { version = "0.1.0", path = "crates/ds-core" }

# Release profile for a short-lived CLI that reads small YAML documents and
# spawns git. Moving bytes is git-lfs's job, so startup dominates runtime.
[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
strip = "symbols"
